<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ feature.name }}</title>
</head>
<body>
    <h1>{{ feature.name }}</h1>
    <p><strong>Description:</strong> {{ feature.description }}</p>
    <p><strong>Benefit:</strong> {{ feature.benefit }}</p>
    <p><strong>Complexity:</strong> {{ feature.complexity }}</p>
    <a href="{{ url_for('home') }}">Back to Features</a>
</body>
</html>

